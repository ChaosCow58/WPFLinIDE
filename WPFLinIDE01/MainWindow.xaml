<Window x:Class="WPFLinIDE01.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFLinIDE01"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        mc:Ignorable="d"
        Title="IDE" Height="800" Width="1200" WindowStartupLocation="CenterScreen">

    <Window.InputBindings>
        <KeyBinding Key="C" Modifiers="Ctrl+Alt" Command="{Binding Show_PowerShell}"/>
    </Window.InputBindings>

    <Grid Name="gMain">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="230"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#5C5C5C" Grid.ColumnSpan="2">
            <Button Content="File" Width="60" Background="Transparent" BorderThickness="0" FontSize="16" Foreground="White" FontWeight="DemiBold"/>
            <Button Content="Edit" Width="60" Background="Transparent" BorderThickness="0" FontSize="16" Foreground="White" FontWeight="DemiBold"/>
            <Button Content="Git" Width="60" Background="Transparent" BorderThickness="0" FontSize="16" Foreground="White" FontWeight="DemiBold"/>
            <Border BorderBrush="DarkGray" BorderThickness="1.5,0,0,0" Height="30" CornerRadius="1"/>
            <Button Background="Transparent" BorderThickness="0">
                <StackPanel Orientation="Horizontal" Margin="13,0,0,0">
                    <Polygon Points="0,0 20,10 0,20" Fill="#8AE28A" VerticalAlignment="Center"/>
                    <Label Content="Run Code" VerticalAlignment="Center" Foreground="LightGray"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <StackPanel Grid.Row="1" Background="#7A7A7A">
            <TextBox x:Name="tbEditor" Height="525" Background="Transparent" BorderThickness="0" FontSize="15" Foreground="Black"/>
        </StackPanel>

        <Grid x:Name="gTermialPanel" Grid.Row="2" Height="220" Background="#616161">
        
        </Grid>

        <StackPanel Name="spFileTree" Grid.Column="1" Grid.Row="1" Grid.RowSpan="2" Background="#707070">
            <TreeView x:Name="tvFileTree" Height="750" Background="Transparent" FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>
