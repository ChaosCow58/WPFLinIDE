<Window x:Class="WPFLinIDE01.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFLinIDE01"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        mc:Ignorable="d"
        Title="IDE" Height="800" Width="1200" WindowStartupLocation="CenterScreen" MouseDown="Window_MouseDown">

    <Window.InputBindings>
        <KeyBinding Key="C" Modifiers="Ctrl+Alt" Command="{Binding ShowPowerShell_Command}"/>
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveFile_Command}"/>
        <KeyBinding Key="F5" Command="{Binding RunCode_Command}"/>
    </Window.InputBindings>

    <Grid Name="gMain">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#5C5C5C" Grid.ColumnSpan="3">
            <Button Content="File" Width="60" Background="Transparent" BorderThickness="0" FontSize="16" Foreground="White" FontWeight="DemiBold" Click="MenuItem_Click">
                <Button.ContextMenu>
                    <ContextMenu>
                        <MenuItem Name="miSaveItem" Header="Save File" Click="miSave_MenuItem_Click" Icon="" InputGestureText="Ctrl+S"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
            <Button Content="Edit" Width="60" Background="Transparent" BorderThickness="0" FontSize="16" Foreground="White" FontWeight="DemiBold"/>
            <Button Content="Git" Width="60" Background="Transparent" BorderThickness="0" FontSize="16" Foreground="White" FontWeight="DemiBold"/>
            <Border BorderBrush="DarkGray" BorderThickness="1.5,0,0,0" Height="30" CornerRadius="1"/>
            <Button x:Name="c" Background="Transparent" BorderThickness="0" Click="btRunCode_Click">
                <StackPanel Orientation="Horizontal" Margin="13,0,0,0">
                    <Polygon Points="0,0 20,10 0,20" Fill="#8AE28A" VerticalAlignment="Center"/>
                    <Label x:Name="lRunCode" Content="Run Code" VerticalAlignment="Center" Foreground="LightGray"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <Grid Grid.Row="1" MinWidth="945" Height="auto" Background="#7A7A7A">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer x:Name="svLineNumbers" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled" CanContentScroll="True">
                <TextBlock x:Name="tbLineNumbers" VerticalAlignment="Top" Height="auto" MinHeight="520" Background="#7A7A7A" Foreground="White" TextAlignment="Right" FontSize="15" Padding="5"/>
            </ScrollViewer>

            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Hidden" ScrollChanged="svEditors_ScrollChanged" VerticalContentAlignment="Top" HorizontalContentAlignment="Left" VerticalAlignment="Top">
                <TextBox x:Name="tbEditor" Grid.Column="1" Height="auto" MinHeight="520" VerticalContentAlignment="Top" HorizontalContentAlignment="Left" VerticalAlignment="Top" HorizontalAlignment="Left" 
                         Background="Transparent" BorderThickness="0" FontSize="15" Foreground="Black" AcceptsReturn="True" AcceptsTab="True" 
                         TextChanged="tbEditor_TextChanged" PreviewMouseWheel="tbEditor_PreviewMouseWheel"/>
            </ScrollViewer>
        </Grid>

        <GridSplitter x:Name="gsTerminalSplitter" Grid.Row="2" DragIncrement="1" Style="{StaticResource CustomGridSplitterStyleHorizontal}" ResizeDirection="Rows" />

        <Grid x:Name="gTermialPanel" Grid.Row="3" Height="auto" MinHeight="220" Background="#616161"/>

        <GridSplitter Grid.Column="1" Grid.Row="1" Width="8" DragIncrement="1" Style="{StaticResource CustomGridSplitterStyleVertical}" HorizontalAlignment="Center" Margin="0,0,0,529"/>

        <TreeView x:Name="tvFileTree" Grid.Column="2" Grid.Row="1" Grid.RowSpan="3" Height="auto" Background="#707070" FontSize="14" MinWidth="230">
            <TreeView.Resources>
                <ContextMenu x:Key="ItemContextMenu_Folder" Visibility="Hidden">
                    <MenuItem Header="Add" Click="ItemRenameMenuItem_Click" InputGestureText=""/>
                    <MenuItem Header="Rename" Click="ItemRenameMenuItem_Click" InputGestureText="F2"/>
                    <MenuItem Header="Delete" Click="ItemDeleteMenuItem_Click" InputGestureText="Del"/>
                </ContextMenu>

                <ContextMenu x:Key="ItemContextMenu_File" Visibility="Hidden">
                    <MenuItem Header="Rename" Click="ItemRenameMenuItem_Click" InputGestureText="F2"/>
                    <MenuItem Header="Delete" Click="ItemDeleteMenuItem_Click" InputGestureText="Del"/>
                </ContextMenu>
            </TreeView.Resources>
        </TreeView>
        
    </Grid>
</Window>
